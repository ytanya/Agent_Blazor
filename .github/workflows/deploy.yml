name: Deploy to Azure Static Web Apps

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Restore
        run: dotnet restore BlazorHero.CleanArchitecture.sln

      - name: Publish Client
        run: dotnet publish "src/Client/Client.csproj" -c Release -o output/client --no-restore

      - name: Publish Server
        run: dotnet publish "src/Server/Server.csproj" -c Release -o output/api --no-restore

      - name: Deploy
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_BRAVE_BAY_09FFEC10_3 }}
          action: "upload"
          app_location: "output/client/wwwroot"
          api_location: "output/api"
          skip_app_build: true
          skip_api_build: true
          app_build_command: "echo 'build skipped'"
          api_build_command: "echo 'build skipped'"
